<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nadershamma.apps.androidfunwithflags">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupOnly="true">
        <activity
            android:name="com.nadershamma.apps.registro.DIAS_Registro"
            android:exported="true"/>
        <activity
            android:name=".DIAS_MainActivity"
            android:exported="true"/>
        <activity
            android:name=".DIAS_LoginActivity"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DIAS_SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".DIAS_MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.nadershamma.apps.androidfunwithflags.DIAS_MainActivity" />
        </activity>
    </application>

</manifest>